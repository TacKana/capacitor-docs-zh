---
title: PWA Elements
description: 使用 PWA Elements
contributors:
  - dotNetkow
  - mlynch
slug: /web/pwa-elements
---

# PWA Elements

某些 Capacitor 插件（如 `Camera` 或 `Toast`）在非原生运行时会提供基于 Web 的 UI。例如，在 Web 上运行时调用 `Camera.getPhoto()` 会加载一个响应式拍照体验：

![PWA Elements](/img/v4/docs/pwa-elements.png)

该 UI 是使用 Web Components 实现的。由于 Shadow DOM 的特性，这些组件不会与您自己的 UI 产生冲突。

## 安装

要启用这些控件，您需要将 `@ionic/pwa-elements` 添加到您的应用中。

典型的安装方式包括导入包并注册元素，或者在应用的 `index.html` 的 `<head>` 中添加 script 标签：

#### 导入 PWA Elements

```bash
npm install @ionic/pwa-elements
```

然后，根据您选择的框架，导入元素加载器并在适当的时机调用：

_React_

`index.tsx` 或 `index.js`：

```tsx
import { defineCustomElements } from '@ionic/pwa-elements/loader';

ReactDOM.render(<App />, document.getElementById('root'));

// 在应用首次渲染后调用元素加载器
defineCustomElements(window);
```

_Angular_

`main.ts`：

```typescript
import { enableProdMode } from '@angular/core';
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { AppModule } from './app/app.module';
import { environment } from './environments/environment';

import { defineCustomElements } from '@ionic/pwa-elements/loader';

if (environment.production) {
  enableProdMode();
}

platformBrowserDynamic()
  .bootstrapModule(AppModule)
  .catch(err => console.log(err));

// 在平台启动后调用元素加载器
defineCustomElements(window);
```

#### 通过 script 标签引入

PWA Elements 可以通过 `index.html` 中的 script 标签引入。但请注意，这在离线场景下无法使用：

```html
<script
  type="module"
  src="https://unpkg.com/@ionic/pwa-elements@latest/dist/ionicpwaelements/ionicpwaelements.esm.js"
></script>
<script
  nomodule
  src="https://unpkg.com/@ionic/pwa-elements@latest/dist/ionicpwaelements/ionicpwaelements.js"
></script>
```
